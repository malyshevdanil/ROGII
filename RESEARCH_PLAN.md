# ROGII — долгосрочная research-программа (цель: пробить кластер 7.2 → 5.6)

Статус: публичный пайплайн ~7.5 (медальный кластер). Быстрый перебор (~20 экспериментов)
исчерпан — все null/negative. Направление ошибки физически отсутствует в данных
(подтверждено ТРИЖДЫ: drift corr −0.08, bimodal r=0.054, PF-residual corr<0.15).
Эта программа — фазированный проект на недели, а не микро-твики.

## Что уже отклонено (не повторять)
Denoise (шум), affine-cal в PF (хуже), resample (шум), NCC-в-PF (хуже), сглаживание
(ошибка=дрейф), deg-2 (шум), cal в beam (хуже), адаптивный scale ×2 (headroom физически
недостижим), overfit-селектора (селектор хорош), NN v1/v2 (переобучение/не обобщается),
shape-matching (value лучше), full DP (~18), traj-DP (катастрофа 69), E1 residual
(направление непредсказуемо).

---

## ФАЗА 0 — Инфраструктура (ускоряет всё остальное в ~50×)
- [~] **0.1 Кэш-харнесс**: посчитать для всех 773 скважин ОДИН раз и сохранить:
      PF-предсказания на scales {3,5,8,12,mean}, PF seed-std/pf-std, beam ensemble,
      flat/linear baseline, true TVT, богатый набор фич, eval-индексы, поле (field-кластер).
      → эксперименты с блендингом/CV/остатками потом за секунды.
- [ ] **0.2 CV-каркас**: GroupKFold по скважине + по полю (block-CV) + случайный.
      Любая идея валидируется одинаково, pooled RMSE, с оценкой сид-шума.

## ФАЗА 1 — Ансамбль/декорреляция — ✅ ПОЗИТИВНЫЙ РЕЗУЛЬТАТ (на PF)
Диагностика: 2 публичных движка ρ≈0.89, нужен ДЕКОРРЕЛИРОВАННЫЙ источник.
**ГЛАВНЫЙ РЕЗУЛЬТАТ**: rich-feature GBM (base+grmatch+signed_azimuth, GroupKFold OOF,
773 скв) как декоррелированный источник:
- PF8 один=11.27, GBM один=14.55, corr ошибок=0.44.
- 0.7·PF+0.3·GBM = **10.56** (−6.3% от PF). Декорреляция работает.
- Ключевая фича: signed azimuth (−0.36 в ablation) — возможно нет у fleongg.
ВАЖНО: это на ИЗОЛИРОВАННОМ PF. Полный пайплайн (7.23) уже блендит fleongg (тоже GBM)
→ выигрыш может быть меньше. УРОК денойза: изолированный тест ≠ полный пайплайн.
Обязательна проверка на реальном LB. Ablation фич: azimuth −0.36 (топ), grmatch −0.31,
tortuosity +0.065 (ВРЕДИТ в контексте, выкинута), selfcorr ~0.
Также: PF-scales между собой corr 0.99; лучший scale=8; hold-к-flat уже в пайплайне.
- [~] **1.1 Каталог декорреляции** (на 50-скв частичном кэше, предварительно):
      PF-scales между собой corr 0.99 (ансамбль по scale бесполезен). PF↔flat 0.32,
      PF↔beam 0.36 (декоррелированы). Лучший блендинг 0.7·pf8+0.3·flat=9.00 vs
      pf8=10.11 standalone. НО: это по сути "hold"-компонента пайплайна (уже ~0.2).
      linear уникален (corr 0.13) но слишком слаб (65 standalone). → повторить на 773.
- [ ] **1.2 Оптимальные веса блендинга** через честный GroupKFold (не подгонка на public).
- [ ] **1.3 Стэкинг**: мета-модель (ridge/LGB) над сигналами с block-CV.
- [ ] **1.4 Свой sequence-GBM** (lag/lead-фичи) как новый декоррелированный член.

## ФАЗА 2 — Робастная агрегация (защита private LB)
- [ ] **2.1 Confidence-weighted откат**: seed_std предсказывает величину ошибки (corr +0.48).
      Когда PF неуверен → сместить к безопасному (linear/flat). Тест: помогает ли на
      худших скважинах, не вредит ли на хороших.
- [ ] **2.2 Robust smoothing худших сегментов** по seed_std-маске.

## ФАЗА 3 — Тяжёлые модели (нужен Kaggle GPU / много компьюта)
- [ ] **3.1 Полное обучение GBM с нуля** на всех 773 (ROGII_TRAIN_FROM_SCRATCH=1),
      честный block-CV, свои фичи. Может дать 2-ю рабочую модель.
- [ ] **3.2 Neural v3 — residual-refiner**: сеть учит ТОЛЬКО там, где PF-остаток
      предсказуем (magnitude-signal), не пытаясь угадать направление. Осторожно с
      переобучением (block-CV, ранняя остановка, крошечная модель).
- [ ] **3.3 Neural v4 — attention с monotonic-constraint + учить на known-зоне как
      pretext**, затем few-shot на eval. (v2 не сошлась — нужен другой режим обучения.)

## ФАЗА 4 — Внешние источники знаний — ✅ ОКУПИЛАСЬ (см. EXTERNAL_TECHNIQUES.md)
Нашли `mycarta/rogii-geosteering-toolkit` — доменные фичи с ablation. ВАЛИДИРОВАНО у нас:
- GBM (LightGBM, GroupKFold) БЬЁТ flat (15.18 vs 15.91) — обобщается там, где NN
  катастрофически переобучалась. GBM = правильный инструмент.
- Q-3D tortuosity (упрощённая) помогает: 15.180 → 15.132 (−0.048; полная версия
  toolkit'а заявлена −0.107). Фича зарабатывает место.
- РЕАЛЬНЫЙ ПУТЬ: rich-feature GBM (tortuosity + dcor + signed azimuth + self-corr +
  offset-prior) как декоррелированный источник → блендинг с PF+physics (Фаза 1).
  Это следующая крупная работа (несколько сессий), но подтверждённо перспективная.

### Детали внешних техник:
- [ ] **4.1 Мониторить Discussion**: топы публикуют write-up ПОСЛЕ конца соревнования.
      Метод сопоставления Tucker (~5) — вот настоящий ключ.
- [ ] **4.2 Геонавигационная литература**: как коммерческий geosteering-софт делает
      GR-корреляцию (dynamic depth matching, взаимная корреляция скользящим окном
      в TVT-пространстве с правильным stretch).
- [ ] **4.3 Изучить soul드rive "Decoding Eagle Ford" ноутбук** (ссылка в диагностике) —
      детальная декомпозиция сложных скважин.

## Протокол
- Held-out: GroupKFold по скважине; для оценки обобщения — block-CV по полю.
- Порог значимости: улучшение держится на ≥100 скважинах (мелкие эффекты = шум).
- Каждый эксперимент → строка в этом файле с результатом (даже negative).
- Хедж-сабмит (2 физических версии, ~7.2-7.5) держим как безопасную базу всегда.
