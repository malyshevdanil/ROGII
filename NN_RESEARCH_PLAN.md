# ROGII — Neural-Network Research Roadmap (путь к топ-1..3, цель ≤5.6)

Цель: заменить/превзойти PF-пайплайн (Public LB 7.096) **робастной моделью, которая генерализует
на локальном whole-well holdout** — как это (предположительно) сделали лидеры (5.2–5.6).
Оптимизируем НЕ на Public LB (там иллюзии от дисперсии PF), а на честном holdout.

## 0. Что уже установлено экспериментами (фундамент, НЕ повторять)
Метрика везде — **pooled RMSE на eval-зоне по held-out скважинам** (160 val, seed 42).
| Подход | Holdout | Вывод |
|---|---|---|
| flat (TVT=last) | **15.1** | ≈ LB flat 15.883 → holdout репрезентативен |
| физика: surface-lin / dip-экстраполяция | 37–85 | тренды НЕ экстраполируются (near-random-walk дрейф) |
| point-wise TCN (регуляриз.) | 15.0 | регрессия по точкам = flat, сигнала не достаёт |
| матчер, статический якорь | 14.95 | continuity схлопывает в flat |
| матчер, едущее окно (iter) | 101 | без монотонности расходится (провал nn_v2) |
| PF-пайплайн | 7.096 LB | sequential Bayes + beam + GBM + gold-калибровка |

**Диагноз:** задача = последовательная байесовская оценка позиции в typewell при сильном
continuity-приоре и СЛАБОМ GR-наблюдении (GR самоподобен, oracle-регистрация ~31ft). Победа —
только в «сладкой точке» между flat-коллапсом и расхождением. PF там уже стоит. NN должна
попасть туда же или точнее.

**Вехи (milestones) на holdout:**
- **M0**: < 14.0 — есть хоть какой-то реальный сигнал (сейчас недостигнуто чистой NN)
- **M1**: ~11 — уровень изолированного PF
- **M2**: ~7 — уровень пайплайна
- **M3**: < 7 → 5.6 — уровень лидеров

---

## Фаза 0+ — доделать инфраструктуру (1–2 сессии, обязательно перед тяжёлыми NN)
Цель — сделать holdout максимально честным и итерацию быстрой.
- **0.1** Вписать реальный PF в holdout-харнесс → знать точную PF-цифру на ЭТИХ 160 скв. (сейчас
  знаем только LB). Даёт прямую цель для NN.
- **0.2** Spatial GroupKFold (по X/Y кластерам), а не случайный сплит — как у лидеров; проверяет
  генерализацию на пространственно-удалённые скважины (главный риск переобучения).
- **0.3** Кэш GR-likelihood-ландшафта как фичи (кросс-корреляция GR-окна с typewell по полосе
  кандидатов) — «PF-подсказка» для любой модели. Тест: даёт ли это M0.
- **0.4** ★ **Перенести тяжёлое обучение на GPU** (Kaggle/Colab). CPU 4 ядра = ~35с/эпоха, это
  прототипирование. Лидеры учили на GPU. Локально — дизайн/дебаг, финальные прогоны — GPU-ноутбук.

---

## Фаза 1 — ГИБРИД / RESIDUAL (самый быстрый ROI, низкий риск) — «стоим на PF»
Не с нуля: NN уточняет уже работающий PF. Реалистичный первый прорыв ниже 7.
- **1.1 Residual-NN**: вход = [PF-предсказание, GR, траектория, seed_std, beam, GBM-сигналы],
  выход = поправка к TVT. Учить на OOF. Риск: как GBM-бленд (провалился) — но с богаче фичами и
  на honest holdout. Критерий: OOF < 7.
- **1.2 Neural likelihood В PF**: заменить гауссову GR-likelihood обученной (CNN-эмбеддинг
  GR-окна vs typewell-окна → скор). Оставить sequential-механику PF (которая работает). ★ Самый
  принципиальный гибрид: чинит слабое звено PF (наивный GR-матч), не ломая continuity.
- **1.3 Neural proposal в PF**: обучаемое распределение движения частиц (вместо MOM/VN/PN) —
  предсказывать шаг дипа из локального GR/траектории.
- **1.4 Learned particle re-weighting/re-ranking**: NN переоценивает гипотезы частиц по глобальному
  контексту (весь ствол сразу), чего онлайн-PF не видит.
- **1.5 Deep stacking**: PF + beam + GBM + NN → мета-модель (Ridge/LGBM/малый MLP) на honest OOF.

## Фаза 2 — НЕЙРО-ФИЛЬТР (главная ставка на «чистый NN», средний риск, высокий потолок)
Мимикрия того, что работает (sequential Bayes), но обучаемое целиком.
- **2.1 GRU/LSTM state-estimator**: скрытое состояние = belief о позиции; идём по eval-точкам
  последовательно; выход = ограниченный шаг дипа (интегрируется в TVT). Teacher-forcing на
  known-зоне (там знаем истину). Anchor: старт = last_tvt. Чинит и flat-коллапс, и расхождение
  (шаг ограничен + рекуррентная память несёт позицию). **Первый кандидат фазы 2.**
- **2.2 Differentiable/Deep Particle Filter**: набор частиц, обучаемые proposal+likelihood,
  reparam/soft-resampling (Gumbel). Прямая нейро-версия PF. Выше потолок, сложнее обучение.
- **2.3 Structured SSM (S4 / Mamba / linear-attention)**: линейное время, длинный контекст,
  стабильнее RNN. Хорош для длинных стволов.
- **2.4 Deep Kalman / Kalman-VAE**: явный латент позиции + обучаемые переходы/эмиссии,
  ELBO-обучение. Даёт калиброванную неопределённость (полезно для бимодальности).
- **2.5 Encoder→autoregressive decoder**: энкодер (GR+typewell+траектория) → AR-декодер TVT-дипов
  с ограничением монотонности/гладкости.

## Фаза 3 — КОНСТРЕЙНЕД-ВЫРАВНИВАНИЕ (нейро-DTW «правильно», средний риск)
Матчинг GR↔typewell, но с ЖЁСТКОЙ монотонностью (провал iter-матчера был из-за её отсутствия).
- **3.1 Soft-DTW (Cuturi-Blondel)**: настоящий дифференцируемый DP-alignment с монотонными
  переходами, эмиссия = CNN-эмбеддинги. Гарантированная монотонность = не разойдётся.
- **3.2 Monotonic attention (Raffel)**: attention с обучаемым монотонным шагом по typewell.
- **3.3 Sinkhorn / OT-выравнивание** с монотонным приором.
- **3.4 CTC-стиль**: позиции typewell = «алфавит», выравнивание горизонтали к последовательности.
- **3.5 Learned cost + классический DP на инференсе** (гибрид: NN учит стоимость, DP решает).

## Фаза 4 — SHAPE-ПРИОР / ГЕНЕРАТИВНЫЕ (эксплуатируем человеческую разметку + бимодальность)
TVT нарисован геологами: 64.5% точек — прямые сегменты (кусочно-линейно), дипы квантованы.
- **4.1 Breakpoint+dip модель**: NN предсказывает точки излома и наклоны сегментов (структурный
  выход под то, как рисовал человек). Регуляризует к правдоподобным формам. (Риск: где изломы —
  плохо детектируется, corr 0.08–0.29; но обучаемо лучше эвристик.)
- **4.2 MDN / MTP (multi-trajectory)**: явно моделируем бимодальность (±15ft Eagle Ford),
  winner-take-all + posterior-mean инференс. (Пробовали → ~15 из-за слабого бэкбона; переделать
  на бэкбоне фазы 2.) 
- **4.3 Diffusion/VAE над TVT-кривыми** conditioned на GR+typewell: сэмплировать правдоподобные
  кривые → posterior mean. Учит саму МЕРУ правдоподобных геологических форм. Высокий потолок.
- **4.4 Score-based prior + likelihood guidance**: диффузионный приор форм TVT + GR-guidance на
  инференсе (аналог SDF из заметок про Grandmaster'а).

## Фаза 5 — СЖАТИЕ / АНСАМБЛИ / ТЕХНИКИ ОБУЧЕНИЯ (ортогонально, применять ко всем)
- **5.1 Аугментация** (ключ к генерализации, тезис лидеров): GR-калибровочный jitter, гаусс-шум,
  time-warp (растяжение MD), channel-dropout, typewell-субсэмплинг, well-mixup, случайная обрезка
  known/eval границы. → перебор силы аугментации = десятки «попыток».
- **5.2 Channel restriction** (прямой намёк лидеров): минимизировать входные каналы, чтобы не
  переобучаться на спурьёзные — сетка по подмножествам каналов.
- **5.3 Self-supervised pretrain**: mask-GR-and-reconstruct; контрастив GR-окно↔typewell-окно
  (учит матчинг без меток) → дообучение. Может дать M0, где supervised не смог.
- **5.4 Multi-task**: доп.голова — предсказать Geology-формацию (есть в typewell) как регуляризатор.
- **5.5 Робастные лоссы**: Huber/quantile/winner-take-all; pooled-RMSE-aware взвешивание.
- **5.6 Ансамбль**: фолды × сиды × архитектуры → усреднение (декоррелированное). На honest holdout.
- **5.7 Curriculum**: сначала короткие eval-зоны, потом длинные (стабилизирует обучение позиции).

---

## Порядок исполнения (прагматичный: сначала вероятное, потом высокий-потолок)
1. **Фаза 0+** (0.1 PF-в-holdout, 0.2 spatial CV, 0.4 GPU) — обязательный фундамент.
2. **Фаза 1.2 (neural likelihood в PF)** и **1.1 (residual-NN)** — самый быстрый реальный <7.
3. **Фаза 2.1 (GRU state-estimator)** — первый честный «чистый NN», цель M1→M2.
4. **Фаза 5.1/5.2 (аугментация+channel restriction)** поверх лучшего из шага 3 — на M2→M3.
5. **Фаза 3.1 (soft-DTW)** и **Фаза 4.3 (diffusion)** — параллельные высокие ставки на <7 → 5.6.
6. **Фаза 5.6 (ансамбль)** лучших моделей — финальный сжим.

## Честная оценка вероятностей (без иллюзий)
- **< 7 достижимо** через Фазу 1 (гибрид на PF) — реалистично, средняя уверенность.
- **~6 (чистый NN)** через Фазу 2/3 с аугментацией — возможно, но нужен GPU и много итераций.
- **5.6 (топ-3)** — требует либо Фазы 4 (генеративный shape-приор, сложно), либо доступа к
  непубличному инсайту лидеров (раскроют после соревнования). Не гарантировано ничем.
- Критический риск на всём пути — **«стена»**: направление дрейфа частью физически не в данных.
  Даже идеальная модель ограничена этим (~40% ошибки = неустранимая физика Eagle Ford).

## Правила исследования (чтобы не сжигать сабмиты, как с 4way)
- Ранжируем ТОЛЬКО на honest whole-well holdout (+ spatial CV). Public LB — не судья.
- Submit на Kaggle только модель, что на holdout **уверенно < 7.096** и стабильна по фолдам/сидам.
- Каждый прогон → строка в `research/RESULTS.jsonl`. Веха достигнута — фиксируем чекпойнт.
- 7.096 + база 7.220 остаются безопасными финалами, пока NN доказательно не побьёт их на holdout.
