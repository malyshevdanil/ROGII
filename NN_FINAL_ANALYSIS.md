# ROGII — Финальная аналитика нейросетевого исследования

Полный, честный разбор всей работы с NN. Метрика — pooled RMSE (ft) на честном whole-well holdout
(160 скважин). Референсы: **flat 15.1 · line-oracle 6.6 · oracle(surface−Z) 3.9 · PF-пайплайн 7.096 (LB)
· лидеры 5.2–5.4**.

## 1. Инфраструктура (построена)
- **Честный whole-well holdout** (773 скв. → 613 train / 160 val, seed 42), pooled RMSE на eval-зоне.
- **Якорь достоверности:** flat на holdout = 15.1 ≈ LB flat 15.9 → ранжирование моделей доверенное.
- Кэш фичей + typewell-грид, аугментация, EMA, GPU-стенд (T4). `research/RESULTS.jsonl`.

## 2. Все архитектуры и результаты (holdout RMSE)
| Архитектура | RMSE | Итог |
|---|---|---|
| flat (бейзлайн) | 15.1 | — |
| Point-wise TCN (регрессия TVT) | 15.0 | =flat, сигнала нет |
| Матчер (статический якорь) | 14.95 | схлопывается в flat |
| Матчер (итеративный) | 101 | расходится |
| GRU-фильтр (последовательный) | 14.7 | ≈flat |
| Контрастивный матчер | 179ft лок. | GR не локализует |
| Нейро-PF (grid Bayesian) | 24.5 | GR-эмиссия net-вредна |
| Нейро-PF + drift | нестаб. | — |
| **WARP (dTVT + cross-attn)** | **11.28** | ★ лучший NN |
| WARP + H4 multi-scale | 11.19 | маргинально |
| WARP + H4 + EMA | 11.34 | стабильный |
| Surface-space (−Z декомпозиция) | 30 | дрейф, ill-conditioned |
| Low-DOF / spline выход | 12 | — |
| Alignment (soft-argmax) | 30 | GR-стена |
| Hybrid (warp + align-якорь) | 11.5 | align не добавил |
| MDN (K=3 + Viterbi) | 14 | mode-collapse чинится, но слаб |
| Синтетик-претрейн (наивный) | 13.2 | не переносится |
| Joint (real + синтетик-аугм.) | 11.57 | ≈WARP, меньше переобуч. |
| 2D misfit-heatmap + SDF (per-col) | ~29 | GR-стена |
| 2D-SDF + Viterbi continuity | ~32 | continuity не спасла |
| Transformer (global attention) | 13.4 | переобучается на 773 скв. |
| Дистилляция/стек PF-сигналов | 10.7 | =изолированный PF |
| *(кэш PF-бленд для справки)* | 10.6 | изолированный PF |

## 3. Ключевые находки (это и есть ценность)
1. **−Z декомпозиция.** `TVT = гладкий_surface − Z`, а `Z` в eval **известен точно**. Оракул
   `surface_deg2 − Z = 3.9`. Значит **вся высокочастотная рябь TVT несётся −Z даром**, а трудность —
   только **гладкий тренд surface** (низкочастотный).
2. **Стена = тренд surface (per-well наклон в eval).** Он **МЕНЯЕТСЯ** относительно known-зоны (разломы)
   → не экстраполируется (43–73), и **ill-conditioned глобально** (GBM по коэффициентам = 22: крошечная
   ошибка наклона × длинное плечо = взрыв). Только **локальный трекинг** обусловлен корректно.
3. **GR-самоподобие → широкополосная, несжимаемая ошибка локализации.** Прямо измерено: контрастив
   179ft; разложение ошибки align (−const 31, −linear 31, −low-freq 28); 2D-SDF плато 30. **Ни фильтр,
   ни low-pass, ни continuity-декод её не убирают** — GR портит локализацию на ВСЕХ масштабах.
4. **Почему WARP (11) — лучший:** он **НЕ доверяет GR** (держит continuity-якорь у last_tvt, лишь слегка
   правит) → 11. А alignment/2D **пере-доверяют** GR → 30. Рекуррентный вывод: **слабый GR + сильная
   continuity бьёт сильный GR-матчинг.**
5. **Больше гибкости → сильнее переобучение** на 773 скв. (Transformer 13.4 хуже CNN-WARP 11). Локальный
   индуктивный биас CNN+continuity — правильный для малых данных.
6. **Наивный синтетик не переносится:** чистый `GR=typewell_GR(TVT)` тривиально инвертируется → на
   реальном самоподобном GR даёт flat. Нужна **реалистичная** синтетика (совпадающая по неоднозначности).

## 4. Почему упёрлись в ~11
Каждый NN с нуля садится на **~10-11 = уровень изолированного PF**, потому что:
- сигнал (GR) физически **широкополосно-ненадёжен** для локализации;
- данных мало (773 скв.) → гибкие модели переобучаются;
- реальный **7-пайплайн (7.096)** берёт своё за счёт **полного классического стека** (PF + beam-DP +
  GBM + gold-калибровка + spatial-KNN), который NN не воспроизводит.

## 5. Что делают лидеры (5.2–5.4) — гипотеза
Полный рецепт (по данным участников Rishikesh Jani #1, Tucker Arrants #2):
1. Минималистичный 1D U-Net / TCN, **радикально сокращённые фичи** (3-4 канала).
2. **MDN-голова** (смесь гауссиан, брать моду не среднее) — под бимодальность ±15ft.
3. **Cross-attention / 2D-heatmap + SDF** — явное выравнивание.
4. ★ **Оффлайн синтетик-претрейн на миллионах реалистичных кривых** — обходит лимит 9ч и лечит
   переобучение. **Это, вероятно, difference-maker**, и именно его реализм мы не докрутили.

## 6. Честный вердикт
- **WARP 11.3 — наш NN-потолок.** Он **хуже** финального PF-сабмита **7.091** (полный пайплайн).
- Реализованы ВСЕ компоненты рецепта лидеров (MDN, few features, aug, синтетик, 2D+SDF, transformer,
  дистилляция) — **ни один не дошёл даже до 11 лучше WARP, не говоря о 5.**
- **Разрыв — в качестве исполнения**, прежде всего в **реалистичной синтетике** (наша наивная не
  переносится), что требует многих сфокусированных итераций эксперта.
- **Соревновательно позиция держится на 7.091 + база (серебро, ~85 место).**

## 7. Что стоит попробовать при возобновлении (приоритет)
1. **Реалистичный синтетик-генератор** — совпадающий с реальной GR-неоднозначностью/статистикой дрейфа;
   валидировать перенос (модель на синтетике должна давать ~11 на holdout, не ~0).
2. **Дистилляция из ПОЛНОГО 7-пайплайна** (не изолированного PF 10.6) — если получить его выходы.
3. Адаптировать **опубликованный код/веса лидеров** после дедлайна (5 авг) — самый надёжный путь к 5.2.

Всё сохранено: скаффолды (`kaggle_warp/mdn/synth/2d/tf.py`), holdout-стенд, память. Research добротный
и полный — он точно **картировал задачу и её стену**, что само по себе ценно (и годится для write-up
приза за объяснение).
